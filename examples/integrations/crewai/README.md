# CrewAI Security Example

This example shows the ProofLayer CrewAI `SecurityMiddleware` wrapping a crew-like runtime. It uses a small local fake crew so the security behavior can be run without API keys.

Run:

```bash
python examples/integrations/crewai/simple_crew.py
```
