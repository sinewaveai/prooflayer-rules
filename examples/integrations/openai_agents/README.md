# OpenAI Agents Security Example

This example shows the ProofLayer `ProofLayerGuardrail` wrapping an agent-like runtime. It uses a small local fake agent so the security behavior can be run without API keys.

Run:

```bash
python examples/integrations/openai_agents/simple_guardrail.py
```
