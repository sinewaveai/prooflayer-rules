# Semantic Kernel Security Example

This example shows the ProofLayer Semantic Kernel `SecurityMiddleware` wrapping a kernel-like runtime. It uses a small local fake kernel so the security behavior can be run without API keys.

Run:

```bash
python examples/integrations/semantic_kernel/simple_kernel.py
```
