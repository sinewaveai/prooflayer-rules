# Pydantic AI Security Example

This example shows the ProofLayer Pydantic AI `SecurityMiddleware` wrapping an agent-like runtime. It uses a small local fake agent so the security behavior can be run without API keys.

Run:

```bash
python examples/integrations/pydantic_ai/simple_agent.py
```
